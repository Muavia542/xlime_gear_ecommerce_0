"use client";
import { useEffect,useMemo,useState } from "react";
import Link from "next/link";
import { api } from "@/lib/api";
import { fmtDate,pretty } from "@/lib/admin";
import { AdminPageHeader,MetricCard,Panel,SearchBox,StatusBadge,EmptyState } from "@/components/admin/AdminUI";
export default function OrdersPage(){const[orders,setOrders]=useState<any[]>([]);const[search,setSearch]=useState("");const[status,setStatus]=useState("ALL");const load=()=>api<any>("/admin/orders").then(r=>setOrders(r.orders||[]));useEffect(()=>{load()},[]);const statuses=["ALL","REQUESTED","CONFIRMED","IN_PRODUCTION","READY_TO_SHIP","SHIPPED","COMPLETED","CANCELLED"];const filtered=useMemo(()=>orders.filter(o=>(status==="ALL"||o.status===status)&&`${o.orderNumber} ${o.customerName} ${o.email} ${o.phone}`.toLowerCase().includes(search.toLowerCase())),[orders,search,status]);return <><AdminPageHeader title="Orders" description="Track requests from submission through production, dispatch and completion."/><div className="adm-grid metrics" style={{gridTemplateColumns:"repeat(5,1fr)"}}><MetricCard label="All" value={orders.length}/><MetricCard label="Requested" value={orders.filter(o=>o.status==="REQUESTED").length} accent="info"/><MetricCard label="Production" value={orders.filter(o=>["IN_PRODUCTION","READY_TO_SHIP"].includes(o.status)).length} accent="warning"/><MetricCard label="Shipped" value={orders.filter(o=>o.status==="SHIPPED").length}/><MetricCard label="Completed" value={orders.filter(o=>o.status==="COMPLETED").length}/></div><div className="adm-toolbar"><SearchBox value={search} onChange={setSearch} placeholder="Order ID, customer, phone…"/><select className="adm-select" value={status} onChange={e=>setStatus(e.target.value)}>{statuses.map(s=><option key={s}>{pretty(s)}</option>)}</select></div><Panel title="Order Desk" subtitle={`${filtered.length} matching orders`}><div className="adm-table-wrap mobile-cards"><table className="adm-table"><thead><tr><th>Order</th><th>Customer</th><th>Date</th><th>Items</th><th>Status</th><th>Action</th></tr></thead><tbody>{filtered.map(o=><tr key={o.id}><td><b>{o.orderNumber}</b></td><td>{o.customerName}<small style={{display:"block",color:"var(--adm-muted)"}}>{o.email}</small></td><td>{fmtDate(o.createdAt)}</td><td>{o.items?.reduce((n:number,i:any)=>n+i.quantity,0)||0}</td><td><StatusBadge value={o.status}/></td><td><Link className="adm-row-link" href={`/admin/orders/${o.id}`}>Open order</Link></td></tr>)}</tbody></table><div className="adm-mobile-card-list">{filtered.map(o=><article className="adm-mobile-record" key={o.id}><header><b>{o.orderNumber}</b><StatusBadge value={o.status}/></header><p>{o.customerName} • {fmtDate(o.createdAt)} • {o.items?.length||0} items</p><Link className="adm-row-link" href={`/admin/orders/${o.id}`}>View order</Link></article>)}</div>{!filtered.length&&<EmptyState title="No orders match"/>}</div></Panel></>}
