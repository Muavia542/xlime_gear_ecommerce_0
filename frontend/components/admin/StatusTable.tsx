"use client";import { api } from "@/lib/api";const statuses=["NEW","REVIEWING","QUOTED","APPROVED","IN_PRODUCTION","COMPLETED","CANCELLED"];
export function RequestTable({rows,endpoint,onChanged}:{rows:any[];endpoint:string;onChanged:()=>void}){return <div className="table-wrap"><table><thead><tr><th>Reference</th><th>Name / Organisation</th><th>Contact</th><th>Status</th><th>Created</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.requestNumber}</td><td>{r.organisation||r.teamName||r.user?.name||"—"}</td><td>{r.email||r.user?.email||"—"}</td><td><select value={r.status} onChange={async e=>{await api(`${endpoint}/${r.id}`,{method:"PATCH",body:JSON.stringify({status:e.target.value})});onChanged()}}>{statuses.map(s=><option key={s}>{s}</option>)}</select></td><td>{new Date(r.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table></div>}
